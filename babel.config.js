module.exports = {
  presets: [
    '@vue/app'
  ],
  // Unambiguous sourceType is only required to link a locally installed package outside of this root directory (during vec development).
  sourceType: 'unambiguous'
}
